# Normalize all text files to LF in the repo
* text=auto eol=lf

# Shell scripts MUST stay LF (CRLF breaks the #!/bin/sh interpreter on macOS/Linux)
*.sh text eol=lf

# PowerShell scripts: CRLF on checkout (Windows-native; PowerShell also reads LF)
*.ps1 text eol=crlf
